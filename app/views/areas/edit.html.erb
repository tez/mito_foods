<% provide(:title, "Edit area") %>
<h1>Update area</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@area) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "Save changes", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>