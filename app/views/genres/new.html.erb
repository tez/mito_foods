<% provide(:title, "New Genre") %>
<h1>New Genre</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@genre) do |f| %>
			<%= render 'fields', f: f %>
      <%= f.submit "Create new genre", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>